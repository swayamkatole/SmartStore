
services:
  mysql:
    image: mysql:8.0
    container_name: smartstore-mysql
    environment:
      MYSQL_ROOT_PASSWORD: smartstore123
      MYSQL_DATABASE: smartstore_db
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

  redis:
    image: redis:7.2
    container_name: smartstore-redis
    ports:
      - "6379:6379"

volumes:
  mysql_data:
